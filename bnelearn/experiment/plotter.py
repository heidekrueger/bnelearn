import matplotlib.pyplot as plt

class Plotter:
    def __init__(self):
        #self.plot_points = min(150, batch_size)
        #self.v_opt = np.linspace(plot_xmin, plot_xmax, 100)
        #self.b_opt = optimal_bid(v_opt)
        pass
